# 完整首轮任务

嵌入式资源加载器需要在固定字节池内分配、调整资源，并拒绝过期句柄。请从零实现“带世代句柄的伙伴内存分配模拟器”。

1. 实现byte arena伙伴分配模拟库，容量2^p，4<=p<=24，最小块16字节。Alloc(size,alignment)要求size>0、alignment为不超过容量的2次幂；对齐相对于arena偏移0，不涉及真实指针。
2. 拆分最小可容纳请求及对齐的块，同阶选最低偏移，释放递归合并伙伴。分配失败不改变可见状态；不暴露底层切片，Read/Write仅拷贝请求长度范围内数据。
3. 句柄含槽号和世代；释放后旧句柄读写/释放失败，地址复用也不复活。世代不得回绕，耗尽则报错。新分配和释放块清零，越界读写失败且不修改数据，禁止unsafe。
4. Resize(newSize>0)保留原alignment，缩小可释放伙伴；增长仅在保持原偏移时原地合并，否则先申请新块再迁移。成功保留前min(old,new)字节、清零增长部分并使旧句柄失效；无新块时允许失败，保留旧句柄/数据/空闲结构。
5. 审计所有已用/空闲块不交叠并覆盖arena，同阶可合并伙伴不遗留。用小池逐字节占用参考验证固定种子操作序列与失败原子性；只做单线程内存模拟，演示迁移及陈旧句柄失败。

验收重点：对齐、拆分合并、碎片失败、重复释放、Read不泄漏可写别名、Resize保留对齐、增长失败回滚、清零。

工程约束：全新0-1核心库，仅用Go 1.26.5标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`go run ./cmd/demo`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`go test ./... -count=1 -timeout=60s`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。
