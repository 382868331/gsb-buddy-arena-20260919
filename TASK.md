# 完整首轮任务

请从零实现伙伴分配器的内存模拟库。调用方通过带世代号的句柄访问数据，释放或调整大小后，旧句柄不能误访问后来分配的块。具体规则如下。

实现byte arena伙伴分配模拟库，容量2^p，4<=p<=24，最小块16字节。Alloc(size,alignment)要求size>0、alignment为不超过容量的2次幂；对齐相对于arena偏移0，不涉及真实指针。

拆分最小可容纳请求及对齐的块，同阶选最低偏移，释放递归合并伙伴。分配失败不改变可见状态；不暴露底层切片，Read/Write仅拷贝请求长度范围内数据。

句柄含槽号和世代；释放后旧句柄读写/释放失败，地址复用也不复活。世代不得回绕，耗尽则报错。新分配和释放块清零，越界读写失败且不修改数据，禁止unsafe。

Resize(newSize>0)保留原alignment，缩小可释放伙伴；增长仅在保持原偏移时原地合并，否则先申请新块再迁移。成功保留前min(old,new)字节、清零增长部分并使旧句柄失效；无新块时允许失败，保留旧句柄/数据/空闲结构。

审计所有已用/空闲块不交叠并覆盖arena，同阶可合并伙伴不遗留。用小池逐字节占用参考验证固定种子操作序列与失败原子性；只做单线程内存模拟，演示迁移及陈旧句柄失败。

测试里请覆盖这些情况：对齐、拆分合并、碎片失败、重复释放、Read不泄漏可写别名、Resize保留对齐、增长失败回滚、清零。

使用 Go 1.26.5 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `go run ./cmd/demo` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `go test ./... -count=1 -timeout=60s` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
