# 完整首轮任务

请从零实现“带世代句柄的伙伴内存分配模拟器”，用于系统编程 / 内存管理中的本地可复用模块。

1. 从零实现本地 byte arena 分配库，容量2^p，4<=p<=24，最小块16字节；不分配真实裸指针。Alloc(size,alignment)返回不透明句柄，size>0，对齐为不超过容量的2次幂。
2. 使用伙伴算法拆分和合并，以能满足大小与对齐的最小块为目标，同阶优先最低偏移；分配失败不能改变可见状态。返回视图只暴露请求长度，不能读写内部碎片或别人的块。
3. 句柄包含槽号和世代，释放后旧句柄读写/释放全部失败，即使地址被复用；世代不得回绕复用。Read/Write 使用拷贝并检查边界，释放清零，禁止 unsafe。
4. Resize 可原地缩小并释放伙伴；增长先尝试合法伙伴合并，不行则迁移，保留前min(old,new)字节并清零新增部分。成功使旧句柄失效并返回新句柄；失败保留旧句柄、数据和空闲结构。
5. 提供审计快照检查已用/空闲块不交叠且覆盖整个 arena、同阶可合并伙伴未遗留；用小容量字节占用参考验证固定种子分配/释放/调整序列。只做单线程模拟器，不包装操作系统内存。

验收重点：反复拆分合并、超大对齐、碎片导致失败、陈旧/重复释放、迁移保真、增长失败回滚、容量恢复。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Go 1.26.5标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；go run ./cmd/demo须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为go test ./... -count=1 -timeout=60s。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
